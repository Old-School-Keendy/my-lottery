<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.keendy.lottery.infrastructure.dao.IStrategyDao">
    <resultMap id="strategy" type="cn.keendy.lottery.infrastructure.po.Strategy">
        <id column="id" property="id"/>
        <id column="strategy_id" property="strategyId"/>
        <id column="strategy_desc" property="strategyDesc"/>
        <id column="strategy_mode" property="strategyMode"/>
        <id column="grant_type" property="grantType"/>
        <id column="grant_date" property="grantDate"/>
        <id column="ext_info" property="extInfo"/>
        <id column="create_time" property="createTime"/>
        <id column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="base_column">
        id, strategy_id, strategy_desc, strategy_mode, grant_type,
        grant_date, ext_info , create_time, update_time
    </sql>

    <select id="selectOneByStrategyId" parameterType="java.lang.Long" resultMap="strategy">
        SELECT
        <include refid="base_column"/>
        FROM strategy
        WHERE strategyId = #{strategyId}
    </select>
</mapper>
